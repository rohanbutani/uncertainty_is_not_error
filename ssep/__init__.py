"""SSEP first pass: generation + capture + labeling (storage schema v1.7).

Script modules `make_pwq_snapshot` / `generate_and_capture` / `compute_labels`
mirror the SEP repo's decomposition; subpackages follow its uncertainty/{data,models,
uncertainty_measures,utils} grouping."""
